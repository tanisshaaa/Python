# ==========================================
# 3. Type Conversion & Type Casting
# ==========================================

# ------------------------------------------
# Easy — 10 Questions
# ------------------------------------------
# 61. Convert an integer to a float using float().
# 62. Convert a float to an integer using int().
# 63. Convert an integer to a string using str().
# 64. Convert a numeric string to an integer using int().
# 65. Convert a numeric string to a float using float().
# 66. Convert an integer to a boolean using bool().
# 67. Convert a boolean to an integer.
# 68. Convert a character to its ASCII value using ord().
# 69. Convert an ASCII value to a character using chr().
# 70. Convert a number to a string and concatenate it with text.

# ------------------------------------------
# Medium — 10 Questions
# ------------------------------------------
# 71. Calculate the average of integer inputs without losing the decimal part.
# 72. Compare integer division and true division.
# 73. Convert user input into int, float and bool values.
# 74. Convert a string containing a decimal number into float and calculate with it.
# 75. Convert a character digit such as '7' into integer 7.
# 76. Convert integer 7 into the character '7'.
# 77. Convert a list of numeric strings into a list of integers.
# 78. Convert a list of integers into strings.
# 79. Demonstrate truthy and falsy conversions using bool().
# 80. Read two numbers as strings and convert them before arithmetic.

# ------------------------------------------
# Hard — 10 Questions
# ------------------------------------------
# 81. Build a calculator that safely converts numeric input before operations.
# 82. Create a program demonstrating string → float → int → string conversion.
# 83. Compare int(), float(), str() and bool() on several values.
# 84. Build a percentage calculator where all input arrives as strings.
# 85. Convert a list of mixed numeric strings into usable numeric values.
# 86. Demonstrate how int() truncates a floating-point value.
# 87. Convert ASCII codes to characters and characters back to codes.
# 88. Build a unit converter that accepts string input and returns numeric results.
# 89. Create a program that handles invalid numeric conversion using try/except.
# 90. Build a conversion utility supporting string, integer, float and boolean representations.
