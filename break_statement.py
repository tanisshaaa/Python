# ==========================================
# 14. break Statement
# ==========================================

# ------------------------------------------
# Easy — 10 Questions
# ------------------------------------------
# 391. Print numbers from 1 to 100 but stop at 50.
# 392. Search for the first occurrence of a target in a list.
# 393. Stop reading numbers when the user enters 0.
# 394. Find the first number divisible by 7.
# 395. Stop processing when a negative number is entered.
# 396. Find the first even number in a sequence.
# 397. Exit a loop after five iterations.
# 398. Stop a multiplication table at 5.
# 399. Find the first vowel in a string.
# 400. Stop a countdown when it reaches 5.

# ------------------------------------------
# Medium — 10 Questions
# ------------------------------------------
# 401. Search a list and break when the target is found.
# 402. Find the first prime number greater than n.
# 403. Create a password loop that breaks on the correct password.
# 404. Create a guessing game that breaks when the guess is correct.
# 405. Find the first number whose square exceeds a limit.
# 406. Read values until a sentinel and break on it.
# 407. Find the first duplicate in a sequence.
# 408. Find the first character that is not a digit.
# 409. Create a menu loop that breaks on Exit.
# 410. Search a nested list for a target and stop the inner search when found.

# ------------------------------------------
# Hard — 10 Questions
# ------------------------------------------
# 411. Search a two-dimensional list and stop the relevant search when a target is found.
# 412. Build a prime-search algorithm using break efficiently.
# 413. Create a pattern generator with a conditional early exit.
# 414. Build a login system with a maximum number of attempts.
# 415. Create a transaction loop that breaks when a balance is insufficient.
# 416. Find the first pair whose sum equals a target.
# 417. Search a string for a multi-character pattern and stop when found.
# 418. Create a command processor that stops on quit.
# 419. Implement a validation loop that exits immediately after valid input.
# 420. Analyze a nested-loop program and determine exactly which loop a break exits.
